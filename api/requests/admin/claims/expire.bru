meta {
  name: Expire claim (admin)
  type: http
  seq: 4
}

post {
  url: {{base_url}}/admin/claims/{{claim_id}}/expire
  body: json
  auth: none
}

headers {
  X-User-Id: {{admin_user_id}}
}

body:json {
  {
    "reason": "Out-of-band expiry for investigation"
  }
}
