meta {
  name: Verify claim (admin)
  type: http
  seq: 2
}

post {
  url: {{base_url}}/admin/claims/{{claim_id}}/verify
  body: json
  auth: none
}

headers {
  X-User-Id: {{admin_user_id}}
}

body:json {
  {
    "reason": "Admin override: certificate validated offline"
  }
}
