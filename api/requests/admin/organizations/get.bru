meta {
  name: Get organization with members (admin)
  type: http
  seq: 3
}

get {
  url: {{base_url}}/admin/organizations/{{organization_id}}
  body: none
  auth: none
}

headers {
  X-User-Id: {{admin_user_id}}
}
