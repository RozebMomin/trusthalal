meta {
  name: List place events (admin)
  type: http
  seq: 3
}

get {
  url: {{base_url}}/admin/places/{{place_id}}/events?limit=50&offset=0
  body: none
  auth: none
}

params:query {
  limit: 50
  offset: 0
}

headers {
  X-User-Id: {{admin_user_id}}
}
