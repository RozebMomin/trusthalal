meta {
  name: Patch place (admin)
  type: http
  seq: 5
}

patch {
  url: {{base_url}}/admin/places/{{place_id}}
  body: json
  auth: none
}

headers {
  X-User-Id: {{admin_user_id}}
}

body:json {
  {
    "name": "Updated Place Name",
    "address": "Updated address"
  }
}
