node_modules/
.expo/
dist/
*.tsbuildinfo
ios/
android/
.env*.local

# Firebase service-account private key (FCM V1 push credentials).
# Contains a real private key — upload it to EAS via `eas credentials`,
# never commit it. Google only lets you download it once at creation.
# NOTE: google-services.json is deliberately NOT ignored — it holds only
# public identifiers and is required at build time.
*-firebase-adminsdk-*.json
service-account*.json

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli